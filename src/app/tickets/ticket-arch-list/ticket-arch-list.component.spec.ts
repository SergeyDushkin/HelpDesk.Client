/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TicketArchListComponent } from './ticket-arch-list.component';

describe('Component: TicketArchList', () => {
  it('should create an instance', () => {
    let component = new TicketArchListComponent();
    expect(component).toBeTruthy();
  });
});
